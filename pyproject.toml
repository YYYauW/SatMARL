[project]
name = "oasis-sat-marl"
version = "0.1.0"
description = "Opportunity-aware asynchronous MARL for large-scale satellite scheduling."
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "gymnasium>=0.29",
]

[tool.setuptools.packages.find]
where = ["src"]
